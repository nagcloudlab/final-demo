apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-api-gateway
  namespace: microservices
spec:
  action: pod-failure
  mode: one
  selector:
    namespaces:
      - microservices
    labelSelectors:
      app: api-gateway
  duration: "30s"
