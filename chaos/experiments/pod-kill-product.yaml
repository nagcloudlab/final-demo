apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-product-service
  namespace: microservices
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - microservices
    labelSelectors:
      app: product-service
  duration: "30s"
  scheduler:
    cron: "@every 2m"
